Place any files in here that you want to be distributed to all PC's.
I place shortcuts to program installers and diagnostics that my techs like to use,
as well as scripts that my users use; like the Install-RADCConnection.PS1 or the 
VPN configuration tool.

The deletion warning is because I update this folder with a robocopy /mirror command.
